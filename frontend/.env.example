# URL của backend. Có thể để trống: vite.config.ts đã proxy /api → http://localhost:8000
# khi chạy dev, nên không cần đặt gì. Chỉ set nếu backend chạy ở cổng/host khác.
VITE_API_URL=http://localhost:8000
